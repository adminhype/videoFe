import { HlsVideoDirective } from './hls-video.directive';

describe('HlsVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new HlsVideoDirective();
    expect(directive).toBeTruthy();
  });
});
