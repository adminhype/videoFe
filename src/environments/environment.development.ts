/**
 * Default environment configuration.
 * This file is typically used for production builds (`ng build`).
 */
export const environment = {
    // Tip: You usually want to set this to `true` for your live server!
    /** Indicates if the application is running in production mode. */
    production: false,
    
    /** The base URL for the live backend API. */
    baseUrl: 'http://localhost:8000/api',
};